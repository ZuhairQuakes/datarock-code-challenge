# Notebook

`geochem_proximity_model.ipynb` is the executed analysis and presentation deliverable.

Run it reproducibly from the repository root with:

```bash
./run.sh
```

The command refreshes the notebook outputs and all generated files under `output/`.
