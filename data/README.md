# Data

`data_for_distribution.csv` is the unchanged dataset supplied by the [Solve Geosolutions / Datarock coding test](https://github.com/Solve-Geosolutions/coding-test).

The file contains drill-hole interval metadata, eight geochemical assay variables, and a `Class` column with labels `A`, `B`, or `?`.
